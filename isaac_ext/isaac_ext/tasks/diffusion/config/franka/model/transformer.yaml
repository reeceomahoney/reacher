_target_: locodiff.models.transformer.DiffusionTransformer
obs_dim: ${obs_dim}
act_dim: ${act_dim}
d_model: 128
nhead: 4
num_layers: 2
T: ${T}
cond_mask_prob: ${cond_mask_prob}
attn_dropout: 0.0
weight_decay: 1e-6
device: ${device}
