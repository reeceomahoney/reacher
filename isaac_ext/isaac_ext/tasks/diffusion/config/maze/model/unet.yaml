obs_dim: ${obs_dim}
act_dim: ${act_dim}
T_cond: ${T_cond}
cond_embed_dim: 64
down_dims: [64, 256, 512]
device: ${device}
cond_mask_prob: ${cond_mask_prob}
weight_decay: 1e-6
cond_predict_scale: False
inpaint: ${policy.inpaint}
